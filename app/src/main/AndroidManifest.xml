<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nahuelpas.cuentabilidad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NuevoGastoActivity"
            android:label="@string/label_nuevo_gasto">
        </activity>
        <activity android:name=".Database.BD_test"/>
        <activity android:name=".SettingsActivity"
            android:label="Settings"/>
        <activity android:name=".NuevaCategoriaActivity"
            android:label="Agregar Categoria"/>
        <activity android:name=".EditarGastoActivity"/>
        <activity android:name=".DetalleCuentaActivity"/>
            <!--intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter-->
        <activity android:name=".NuevaCuentaActivity"
            android:label="Agregar Cuenta"/>
        <activity android:name=".CuentasActivity"
            android:label="Cuentas"/>
        <activity android:name=".DetalleGastoActivity"/>


    </application>

</manifest>