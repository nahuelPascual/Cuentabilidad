<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nahuelpas.cuentabilidad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.layout.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.layout.NuevoGastoActivity"
            android:label="@string/label_nuevo_gasto"
            android:noHistory="true">
        </activity>
        <activity android:name=".Database.BD_test"/>
        <activity android:name=".View.layout.SettingsActivity"
            android:label="Settings"/>
        <activity android:name=".View.layout.NuevaCategoriaActivity"
            android:label="Agregar Categoria"
            android:noHistory="true"/>
        <activity android:name=".View.layout.EditarGastoActivity"
            android:noHistory="true"/>
        <activity android:name=".View.layout.DetalleCuentaActivity"
            android:noHistory="true"/>
            <!--intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter-->
        <activity android:name=".View.layout.NuevaCuentaActivity"
            android:label="Agregar Cuenta"
            android:noHistory="true"/>
        <activity android:name=".View.layout.CuentasActivity"
            android:label="Cuentas"/>
        <activity android:name=".View.layout.DetalleGastoActivity"
            android:noHistory="true"/>
        <activity android:name=".View.layout.EstadisticasActivity"
            android:label="Estadísticas"/>


    </application>

</manifest>