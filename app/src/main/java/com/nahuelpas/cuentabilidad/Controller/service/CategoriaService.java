package com.nahuelpas.cuentabilidad.Controller.service;


public class CategoriaService {
/*
    MovimientoDao movimientoDao = new GastoDao_Impl(Database.getAppDatabase(MainActivity.APP_CONTEXT));

    public double totalGastos(Categoria categoria) {
        if(categoria.getSubcategorias().isEmpty()){
            return totalGastosCategoria(categoria);
        } else {
            double total = 0;
            for (Categoria cat : categoria.getSubcategorias()) {
                total += totalGastos(cat);
            }
            return total;
        }
    }

    private double totalGastosCategoria(Categoria categoria) {
        double total = 0;
        for(double parcial : movimientoDao.getMontosByCategoria(categoria.getCodigo())){
            total += parcial;
        }
        return total;
    }
    */
}
